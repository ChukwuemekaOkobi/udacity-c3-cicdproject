---
- name: Extract artifact
  unarchive:
    src: ~/project/artifact.tar.gz
    dest: /home/ubuntu

- name: "server"
  become: true
  shell: |
    npm install
    pm2 stop default
    pm2 start npm --start